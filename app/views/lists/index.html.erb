<h1>Your ToDo Lists</h1>

<% @lists.each do |list| %>
<table>
<tr>
  <th><%= list.title %></th>
  <th><%= link_to 'See', list_path(list.id) %>
  <th><%= link_to 'Add', new_list_task_path(list.id) %>
  <th></th>
<% end %>
</tr>
</table>
<br>
<%= link_to 'New List', new_list_path %>
